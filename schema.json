{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "properties": {
    "tsConfig": {
      "type": "array",
      "description": "TypeScript config file(s) e.g. tsconfig.app.json or just tsconfig.json"
    }
  }
}
